# ============================================
# GOALHUB ENVIRONMENT CONFIGURATION
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to backend/.env for the FastAPI backend
# 2. Replace ALL placeholder values with your actual credentials
# 3. NEVER commit the .env file to version control
# 4. Keep this file secure and never share it publicly
# ============================================
#
# NOTE: This project uses FastAPI (Python) backend in backend/ directory
# The Node.js backend (server.js) is deprecated and will be removed
# See PAYMENT_MIGRATION.md for migration details
# ============================================

# ============================================
# APPLICATION ENVIRONMENT (FastAPI Backend)
# ============================================
NODE_ENV=development                    # Options: development, production
PORT=8000                              # FastAPI backend port (default: 8000)

# ============================================
# FIREBASE CONFIGURATION (Required for Google Sign-In)
# ============================================
# SETUP STEPS:
# 1. Go to Firebase Console: https://console.firebase.google.com
# 2. Create a new project or select existing one
# 3. Go to Project Settings (gear icon) → General → Your apps
# 4. Click "Add app" → Web (</>)  → Register app
# 5. Copy the firebaseConfig values below
#
# ENABLE GOOGLE SIGN-IN:
# 1. In Firebase Console → Authentication → Sign-in method
# 2. Click "Google" → Enable → Save
# 3. Add authorized domains (localhost for dev, your domain for prod)
#
# Get config from: Project Settings → Your apps → Firebase SDK snippet → Config

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# ============================================
# M-PESA DARAJA API CONFIGURATION
# ============================================
# Get these from Safaricom Daraja Portal (https://developer.safaricom.co.ke)
# My Apps → Your App → Keys

# SANDBOX CREDENTIALS (For Development/Testing)
# Get from: https://developer.safaricom.co.ke/MyApps
MPESA_CONSUMER_KEY=your_sandbox_consumer_key
MPESA_CONSUMER_SECRET=your_sandbox_consumer_secret
MPESA_PASSKEY=bfb279f9aa9bdbcf158e97dd71a467cd2e0c893059b10f78e6b72ada1ed2c919
MPESA_SHORTCODE=174379

# M-PESA ENVIRONMENT
# Options: sandbox (for testing), production (for live)
MPESA_ENV=sandbox

# PRODUCTION CREDENTIALS (Uncomment and fill when going live)
# MPESA_CONSUMER_KEY=your_production_consumer_key
# MPESA_CONSUMER_SECRET=your_production_consumer_secret
# MPESA_PASSKEY=your_production_passkey
# MPESA_SHORTCODE=your_production_shortcode
# MPESA_ENV=production

# ============================================
# M-PESA CALLBACK URL
# ============================================
# IMPORTANT: M-Pesa callbacks require a publicly accessible HTTPS endpoint
#
# DEVELOPMENT:
# - Leave blank to use http://localhost:8000/api/callback (default)
# - For testing with real M-Pesa, use ngrok or similar tunnel:
#   1. Install ngrok: https://ngrok.com/download
#   2. Run: ngrok http 8000
#   3. Copy HTTPS URL and append /api/callback
#   4. Set CALLBACK_URL=https://your-ngrok-url.ngrok.io/api/callback
#
# PRODUCTION:
# - Set to your public HTTPS domain with /api/callback path
# - Example: CALLBACK_URL=https://api.goalhub.ke/api/callback
# - Update this URL in Safaricom Daraja Portal (MyApps → Your App → URLs)
#
CALLBACK_URL=

# ============================================
# SECURITY CONFIGURATION
# ============================================
# JWT Secret for authentication (if implementing JWT)
# JWT_SECRET=your_secure_random_string_here

# Session secret (if using sessions)
# SESSION_SECRET=your_secure_random_string_here

# ============================================
# SUPABASE POSTGRESQL CONFIGURATION (FastAPI Backend)
# ============================================
# Get connection string from Supabase:
# 1. Go to Supabase Dashboard → Project Settings → Database
# 2. Find "Connection string" under "Connection pooling"
# 3. Copy the URI and replace [YOUR-PASSWORD] with your database password
# 4. Ensure it uses postgresql+asyncpg:// protocol for FastAPI
#
# Format: postgresql+asyncpg://postgres.PROJECT_ID:[PASSWORD]@aws-0-REGION.pooler.supabase.com:5432/postgres
#
DATABASE_URL=postgresql+asyncpg://postgres.PROJECT_ID:[YOUR-PASSWORD]@aws-0-REGION.pooler.supabase.com:5432/postgres

# ============================================
# OPTIONAL: EMAIL/SMS NOTIFICATIONS
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_FROM=noreply@goalhub.ke

# SMS API (Africa's Talking, Twilio, etc.)
# SMS_API_KEY=your_sms_api_key
# SMS_USERNAME=your_sms_username

# ============================================
# LOGGING & MONITORING
# ============================================
# LOG_LEVEL=info                        # Options: error, warn, info, debug
# SENTRY_DSN=your_sentry_dsn            # For error tracking

